query Folder ($projectId: String!, $folderId: String!) {
  folder (projectId: $projectId, folderId: $folderId) {
    data {
      attributes {
        extension {
          type
        }
        displayName
      }
      id
    }
    error {
      message
    }
  }
}