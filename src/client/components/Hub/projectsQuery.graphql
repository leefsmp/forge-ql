query ProjectsQuery ($hubId: String!) {
  projects (hubId: $hubId) {
    data {
      attributes {
        extension {
          type
        }
        name
      }
      id
    }
    error {
      message
    }
  }
}