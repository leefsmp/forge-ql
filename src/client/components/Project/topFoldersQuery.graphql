query TopFolders ($hubId: String!, $projectId: String!) {
  topFolders (hubId: $hubId, projectId: $projectId) {
    data {
      attributes {
        extension {
          type
        }
        displayName
      }
      id
    }
    error {
      message
    }
  }
}